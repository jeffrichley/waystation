# Waystation

A bare Python library for orchestrating sandboxed AI coding agents against git
repositories. Flows are plain Python owned by the user; waystation supplies the
primitives.

See `CONTEXT.md` for vocabulary, `docs/adr/` for decisions, and
`docs/log-records.md` for what a run logs and how to watch it.
