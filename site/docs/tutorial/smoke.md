# Outside the ladder: a smoke run

A `ScriptedAgent` in a throwaway repo, landing on a named branch. It needs no Docker and no credential, so `just smoke` checks the library end to end in a second, and it shows how to test a flow of your own without spending tokens.

```python title="examples/smoke_collect.py"
--8<-- "examples/smoke_collect.py"
```
